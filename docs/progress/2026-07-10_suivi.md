# Suivi - Avancée du 10 juillet 2026

## Réalisations
- Écrit le module d'audit (`src/validation/audit.py`). Il applique toutes les règles de la fiche d'audit, journalise chaque vérification, écrit le rapport `outputs/tables/audit_report.csv` et bloque le pipeline dès qu'une règle échoue.
- Écrit tous les modules d'extraction. Couches de référence (`reference_layers.py`), zone d'étude (`study_area.py`), recensement (`census.py`, lecture par morceaux du fichier de 6,5 Go avec cache filtré dans `data/processed`), transport exo (`transit.py`), réseau piéton (`network.py`), services (`services.py`) et bâtiments résidentiels (`buildings.py`, la valeur `yes` est croisée avec l'usage résidentiel de la CMM).
- Écrit tous les modules d'analyse. Distances de marche multi sources (`graph.py`), cote par type de service (`accessibility.py`), accès au transport (`transit_access.py`), demande pondérée (`demand.py`), couverture par aire de diffusion (`coverage.py`), sites candidats (`candidate_sites.py`) et couverture maximale avec spopt (`optimization.py`).
- Écrit la détection des liens qui traversent la rivière (`bridges.py`). Le retrait de ces liens permet de chiffrer l'effet de barrière.
- Écrit les sorties. Tableaux CSV (`metrics.py`), cartes interactives S0 et S1 (`maps.py`), courbe de gain et couverture par type (`charts.py`).
- Écrit les deux points d'entrée. `download_data.py` vérifie les sources manuelles puis télécharge les couches OSM, `main.py` enchaîne tout le pipeline dans l'ordre du schéma.
- Porté la suite de tests à 37 tests, tous verts, avec ruff et black sans reproche.

## Vérifications sur les données réelles
- Modules d'extraction validés localement sans réseau. Les 4 municipalités se chargent et se dissolvent correctement (zone de demande de 70,8 km², zone d'extraction de 79,8 km²), le sol fusionné donne 4283 polygones, le GTFS donne 387 arrêts fixes une fois le service à la demande écarté, les 2 gares et les 5 lignes dédoublonnées se chargent, et 91 aires de diffusion tombent dans la zone d'extraction.

## Décisions prises
- **Cache du profil du recensement (10 juillet 2026).** Le fichier source de 6,5 Go est filtré une seule fois puis mis en cache dans `data/processed`. Les données brutes restent intactes et le cache se régénère en le supprimant.
- **Demande agrégée par aire de diffusion pour l'optimisation (10 juillet 2026).** L'indicateur qui pilote l'optimisation est par aire de diffusion, chaque aire devient donc un point de demande pondéré par ses aînés non couverts, ce qui garde le modèle de couverture maximale rapide et lisible.
- **Effet de barrière par retrait des liens traversants (10 juillet 2026).** Les rives sont définies par les limites municipales, tout lien du graphe qui relie les deux rives traverse la rivière. Comparer la couverture avec et sans ces liens chiffre l'effet de barrière sans nouvelle source de données.

## Prochaines étapes
1. Lancer `python download_data.py` pour télécharger les couches OSM (réseau, services, bâtiments, adresses).
2. Lancer `python main.py` pour produire les premiers résultats S0 et S1, vérifier le taux de jointure du recensement et le rapport d'audit.
3. Contrôler les couches intermédiaires dans QGIS et la liste des ponts détectés (`outputs/tables/ponts_traversants.csv`) contre l'imagerie aérienne.
4. Préparer la présentation orale avec la carte S0, la courbe de gain et l'effet de barrière.
